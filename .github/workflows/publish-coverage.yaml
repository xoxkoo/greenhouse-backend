name: Test Coverage Deployment

on:
  push:
    branches:
      - dev-TestCoverageWorkflow

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build and run tests
        run: |
          dotnet build
          dotnet test --collect:"XPlat Code Coverage" --results-directory:"./UnitTest/TestResults/83ff94a8-74ae-45bc-bba9-8cfbddfaf090"  # Replace with your MSTest command

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./UnitTest/TestResults/83ff94a8-74ae-45bc-bba9-8cfbddfaf090
          publish_branch: gh-pages